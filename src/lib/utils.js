// The RN app is a "app" (no SSR). Pages Router + client components both work —
// we keep the classic `Page.getLayout` hook available for parity with future
// work. Nothing to do here beyond documentation.
